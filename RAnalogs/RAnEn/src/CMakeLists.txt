cmake_minimum_required (VERSION 2.8)
project (RAnEn_SRC LANGUAGES NONE)
# this script does nothing but tell the upper level CMakeLists.txt that
# this directory is a valid directory
# so we can delete the copied files in this directory directly from the 
# upper level CMakeLists.txt
# to keep this directory as clean as possible
